/* #include <stdio.h>
#include <stdlib.h>

struct Tarea{
    int TareaID; //Numerado en ciclo iterativo
    char *Descripcion; //
    int Duracion; // entre 10 – 100
} typedef Tarea;

struct Nodo{
    Tarea T;
    Nodo *Siguiente;
} typedef Nodo;

void eliminarNodo(Tnodo * nodo);

int main(){

}

void insertarAlFinal(Tnodo * Start, int idBuscado){
    Tnodo * Aux  = Start;
    while()
}

void eliminarNodo(Tnodo * nodo){
    if (nodo)
    {
        if(nodo->Tarea.descripcion){
            free(nodo->Tarea.descripcion);
        }
        free(nodo);
    }
    
} */